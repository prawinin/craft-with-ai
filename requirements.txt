# Python for AI — Dependencies
# Install all at once: pip install -r requirements.txt
#
# Note: The browser runner uses only Python standard-library modules.

# Lesson 16 — Working with APIs
requests>=2.28

# Lesson 17 — Working with Data
pandas>=1.5
matplotlib>=3.6

# Module 3 — Service Layers & APIs (FastAPI & Pydantic)
fastapi>=0.95
pydantic>=2.0
uvicorn>=0.20

# Module 5 & 6 — Vector DBs & LLMs
chromadb>=0.4.0
python-dotenv>=1.0.0
